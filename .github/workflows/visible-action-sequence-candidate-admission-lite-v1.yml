name: Visible Action Sequence Candidate Admission Lite V1

on:
  pull_request:
    paths:
      - "visible_action_sequence_candidate_admission_lite.py"
      - "hpfa/modules/core/visible_action_sequence_candidate_admission_lite/**"
      - "docs/contracts/visible_action_sequence_candidate_admission_lite_v1.md"
      - "tools/run_active_match_visible_action_sequence_candidate_admission_v1.sh"
      - "tools/bootstrap_termux_visible_action_sequence_candidate_admission_v1.sh"
      - ".github/workflows/visible-action-sequence-candidate-admission-lite-v1.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: visible-action-sequence-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
      - name: Exact PR head gate
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          actual_sha="$(git rev-parse HEAD)"
          test "$actual_sha" = "$EXPECTED_SHA"
          echo "exact_head_sha=$actual_sha"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check pytest
      - name: Compile
        run: >-
          python -m compileall -q
          visible_action_sequence_candidate_admission_lite.py
          hpfa/modules/core/visible_action_sequence_candidate_admission_lite
      - name: Focused tests
        run: >-
          python -m pytest -q
          hpfa/modules/core/visible_action_sequence_candidate_admission_lite/tests
      - name: Selected event producer compatibility tests
        run: >-
          python -m pytest -q
          hpfa/modules/core/selected_event_consequence_surface_lite/tests
      - name: Remaining upstream compatibility tests
        run: >-
          python -m pytest -q
          hpfa/modules/core/selected_action_consequence_surface_lite/tests
          hpfa/modules/core/cross_role_relation_candidate_resolver_lite/tests
          hpfa/modules/core/action_bundle_multi_family_review_taxonomy_lite/tests
          hpfa/modules/core/semantic_role_action_bundle_candidates_lite/tests
          hpfa/modules/core/match_local_identity_candidates_lite/tests
          hpfa/modules/core/evidence_atom_inventory_lite/tests
          hpfa/modules/core/row_nucleus_inventory_lite/tests
      - name: Shell syntax
        run: >-
          bash -n
          tools/run_active_match_visible_action_sequence_candidate_admission_v1.sh
          tools/bootstrap_termux_visible_action_sequence_candidate_admission_v1.sh
      - name: Contract JSON
        run: python -m json.tool hpfa/modules/core/visible_action_sequence_candidate_admission_lite/contract/visible_action_sequence_candidate_admission_lite_v1.json >/dev/null
      - name: No sample match identity leak
        run: |
          ! grep -RInE "Australia|Turkey|World Cup|Juventus|Galatasaray|6935|77798" \
            visible_action_sequence_candidate_admission_lite.py \
            hpfa/modules/core/visible_action_sequence_candidate_admission_lite/src \
            docs/contracts/visible_action_sequence_candidate_admission_lite_v1.md \
            tools/run_active_match_visible_action_sequence_candidate_admission_v1.sh \
            tools/bootstrap_termux_visible_action_sequence_candidate_admission_v1.sh
