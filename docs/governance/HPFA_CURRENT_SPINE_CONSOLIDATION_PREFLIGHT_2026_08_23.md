# HPFA Current Spine Consolidation Preflight — 2026-08-23

Status: `POLICY_CORRECTION_PASS / CURRENT_STATE_REFETCHED / INTELLIGENCE_CORRECTNESS_DEBT_ENGINEERING_CLOSED / CONSOLIDATION_PREFLIGHT_READY / NOT_PRODUCTION / NOT_MERGED`

## Purpose

This record fixes the controlled-mainline consolidation boundary against the current GitHub state observed on 2026-08-23. It is governance/integration evidence only. It creates no Football Intelligence truth and carries no ACTIVE_MATCH authority.

## Current authority split

```text
PRODUCT_MAIN=main
main_head=105539970ffd0ca8b5d592a68e800da6057e3274
main_head_date=2026-08-19

CURRENT_DEVELOPMENT_FRONTIER_PR=278
current_development_frontier_branch=test/intelligence-e2e-contract-fixture-v1
current_development_frontier_head=33ebcc161576e0e11012cc8f3c221512013c77f2
pr_278_state=open_draft
pr_278_mergeable=true
pr_278_current_head_ci=SUCCESS

ACTIVE_MATCH_AUTHORITY=runtime/active_single_match/current
ACTIVE_MATCH_CURRENT_REVALIDATION=REQUIRED_ON_COHERENT_INTEGRATED_HEAD

canonical_event_count=UNKNOWN
true_action_count=UNKNOWN
production_release=false
```

`main` and the current development frontier remain different maturity surfaces. Neither may silently impersonate the other.

## Current product-development spine to preserve

### Reconstruction spine

```text
#254 Row Nucleus
→ #259 Evidence Atom
→ #260 Match-Local Identity
→ #261 Semantic Role / Action Bundle
→ #262 Multi-Family Taxonomy
→ #263 Cross-Role Relation
→ #264 Trackable Action Trace
→ #265 Trackable Action Consequence
→ #266 Visible Action Sequence
→ #267 Partial-Order Hardening
```

Partial-order vocabulary remains:

```text
BEFORE_CONFIRMED
AFTER_CONFIRMED
SAME_TIME_UNORDERED
ORDER_INDETERMINATE
PROVENANCE_ORDER_ONLY
```

Source row order is not football temporal truth.

### Intelligence correctness hardening spine

```text
#270 Packet → Fusion upstream failure propagation
→ #271 Packet/Fusion recursive forbidden-field guard
→ #272 Argument recursive forbidden-field guard
→ #273 Argument → Defeasible Route → Evidence Graph canonical route
→ #274 Safe Router review continuity
→ #275 Report Block / Output Contract review continuity
→ #276 remaining recursive guards: Defeasible Router + Assembly
→ #277 Packet → Fusion explicit counter-evidence metadata lineage
→ #278 end-to-end Intelligence contract fixture
```

Exact-head #278 workflow:

```text
Intelligence E2E Contract Fixture V1
run_id=32646584640
conclusion=success
```

The current stacked line now verifies real producer-output → next-consumer continuity, failure propagation, review continuity, recursive nested claim/truth blocking, explicit counter-evidence lineage, `canonical_event_count=UNKNOWN`, and sample-match identity isolation.

Closure record:

`docs/audits/INTELLIGENCE_INTEGRATION_DEBT_CLOSURE_2026_08_23.md`

## Integration debt still open

The repository still contains multiple open historical/current paths for overlapping capability families:

- Foundation and source semantics/reconciliation;
- legacy Evidence Atom / identity / semantic-action paths;
- current #254→#267 reconstruction path;
- historical selected-action / sequence / phase paths;
- spatial/progression branches;
- current #270→#278 Intelligence hardening stack;
- stale controlled-integration branches.

Therefore the remaining P0 debt is now **mainline consolidation and authority normalization**, not another correctness patch and not another new football feature.

Consolidation must not be a chronological PR merge train.

## Current decision

Landing unit remains:

```text
FINAL_CAPABILITY_SNAPSHOT
```

Forbidden shortcuts:

```text
giant merge
blind full-stack rebase
historical PR merge train
parallel replacement engine when current producer can be safely extended
stale runtime evidence promoted to current exact-head evidence
copying donor modules as product truth
```

## Required consolidation order

### C0 — Authority and inventory normalization

- refresh machine-readable `PRODUCT_MAIN / DEVELOPMENT_CHECKPOINT / ACTIVE_MATCH_RUNTIME` separation;
- classify open PRs/capabilities as CURRENT_CAPABILITY, SUPERSEDED_HISTORICAL, DONOR_SUPPORT, SPEC_BACKLOG or CLOSE_CANDIDATE;
- identify exact final-state file lineage instead of PR chronology;
- preserve current main history and avoid destructive branch rewrites;
- keep merge/release/production decisions separate.

### C1 — Final Foundation snapshot

```text
inventory
→ CSV reader
→ XLSX reader
→ XML reader
→ content/source-role resolution
→ provider field semantics
→ provider label/value semantics
→ cross-format reconciliation
→ metric-definition policy
→ aggregate-definition alignment
→ provider metric dictionary where required
→ Row Nucleus / G01–G18 final behaviour
```

Later corrections must be folded into the layer they repair rather than landing an older historical snapshot.

### C2 — Current Evidence Spine snapshot

```text
Row Nucleus
→ Evidence Atom
→ Match-Local Identity
→ Semantic Role / Action Bundle
→ Multi-Family Taxonomy
→ Cross-Role Relation
```

### C3 — Football Reconstruction + Intelligence hardening snapshot

```text
Trackable Action Trace
→ Trackable Action Consequence
→ Visible Action Sequence
→ Partial-Order Hardening
→ current Intelligence correctness hardening
→ end-to-end contract fixture
```

The #270→#278 corrections are folded into the current final module files they repair; they are not replayed as a historical PR train.

### C4 — Integrated exact-head revalidation

After one coherent integration head exists:

```text
CI
→ contract/invariant audit
→ Intelligence E2E fixture
→ no-sample-match-identity-leak guard
→ flat phone-output guard where applicable
→ exact-head ACTIVE_MATCH execution where product/runtime behavior is affected
→ Engineering Evidence
→ Analyst Evidence
→ claim-boundary audit
```

No downstream feature frontier is promoted before C0–C4 are complete.

## Lens / orchestration debt

Evidence Lens Matrix is currently an explicit review sidecar over Evidence Graph; it is not the direct Safe Router input. Missing lenses remain `REVIEW_REQUIRED` and `absence_inference_allowed=false`.

A small integrated orchestration/gating slice is still required so Lens review state and the primary reasoning/output path cannot diverge at production-bound assembly. This should be built **after** the corrected current capability has been consolidated onto a coherent integration head, rather than extending the already-deep feature stack.

## Post-consolidation first new football-intelligence frontier

After consolidation and orchestration-gating closure:

```text
Context Evidence Re-binding
→ Analyst Episode Locator
→ Rhythm / Change Detection
→ Recurrence / Variation / Deviation
```

Historical Match Context Slicer and Termux apparatus remain `DONOR_SUPPORT / CAPABILITY_RECOVERY_RESERVE` and must be adapted to the current spine under `ADAPT_NOT_COPY`.

## Claim boundary

```text
research_evidence != implementation_evidence
implementation_evidence != ACTIVE_MATCH_evidence
surface_rows != canonical_events
sequence_candidate != sequence_truth
partial_order != directly_follows_truth
context_candidate != tactical_truth
canonical_event_count=UNKNOWN
true_action_count=UNKNOWN
production_release=false
```

No merge, auto-merge, release or production binding is authorized by this document.
