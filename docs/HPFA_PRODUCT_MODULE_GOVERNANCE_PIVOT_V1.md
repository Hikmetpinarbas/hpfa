# HPFA Product Module Governance Pivot V1

## Coordinator Decision

HPFA must now be managed as product modules, not individual files and not raw capability rows.

The asset inventory phase is complete.
The capability consolidation phase is active.
The next maturity jump is product-module governance.

## Core Chain

```text
Capability Family
→ Composite Apparatus
→ Product Module
→ Product Release
→ Professional Football Intelligence
```

## Immediate Product Target

```text
POSTMATCH_RELEASE_0.1
```

## Product Modules

Initial module taxonomy:

```text
ACTIVE_MATCH_RUNTIME
INGESTION_ENGINE
CANONICAL_EVENT_ENGINE
ACTION_ENGINE
SEQUENCE_ENGINE
PROGRESSION_ENGINE
CONSEQUENCE_ENGINE
EVIDENCE_ENGINE
CLAIM_ENGINE
DIAGNOSTIC_ENGINE
REPORT_ENGINE
AUDIT_ENGINE
EXTERNAL_DONOR_GOVERNANCE
```

## External Ecosystem Governance

External source records must include:

```text
trust_level
update_frequency
import_policy
allowed_artifact_types
runtime_allowed
claim_allowed
integration_stage
```

Default policy:

```text
GitHub       = canonical code authority candidate
Dropbox      = donor extraction only / package archive
Google Drive = constitution / registry / publication standard authority
Termux       = execution lab and runtime evidence
ACTIVE_MATCH = only active match runtime authority
```

## Runtime Rule

No Dropbox or Google Drive file is runtime authority.
No PDF is event truth.
No GitHub code is production-ready until Termux ACTIVE_MATCH execution proves it.

## Current State

`hpfa_capability_consolidation_review_v1` is PASS and produces reviewed capability families.

Next operational node:

```text
hpfa_product_module_registry_v1
```

## Next Node Purpose

Map reviewed capability families into product modules and define Release 0.1 module scope.

## Release Rule

A product module can enter Release 0.1 only if it has:

```text
capability_family
composite_or_best_runtime_candidate
provenance_paths
claim_boundary
runtime_status
release_status
```

## Strict Rules

- Do not return to asset inventory.
- Do not treat files as product modules.
- Do not bind dirty candidates.
- Do not select PDF/reference as runtime.
- Do not promote quarantine/archive paths.
- Do not delete variants before composite execution proof.
- Do not use external donors as runtime authority.
